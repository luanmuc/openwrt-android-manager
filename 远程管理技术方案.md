# OpenWrt管家 - 远程管理功能技术方案

**版本**：v1.0
**日期**：2026-08-12
**状态**：已实现基础版本（DDNS + 端口映射）

---

## 一、需求分析

用户希望在外网（不在同一个局域网）也能通过APP管理OpenWrt路由器。

### 核心需求
1. 在外网通过互联网连接路由器
2. 自动检测网络环境，智能切换本地/远程模式
3. 配置简单，安全可靠
4. 支持多种远程接入方案

---

## 二、技术方案对比

### 方案1：DDNS + 端口映射（推荐基础版本）

#### 原理
```
[手机APP] --互联网--> [DDNS域名:端口] --端口映射--> [路由器LuCI]
```

#### 优点
- 实现简单，不需要额外服务
- 延迟低，直连路由器
- 成本低，免费DDNS即可
- 兼容性好，所有OpenWrt都支持

#### 缺点
- 需要公网IP（部分运营商不提供）
- 开放端口到公网有安全风险
- 需要配置DDNS和防火墙

#### 适用场景
- 有公网IP的用户
- 对延迟敏感的用户
- 技术能力较强的用户

#### 实现难度：⭐⭐（简单）

---

### 方案2：内网穿透（frp/ngrok）

#### 原理
```
[手机APP] --互联网--> [frp服务器] --隧道--> [路由器frp客户端] ---> [LuCI]
```

#### 优点
- 不需要公网IP
- 可以穿透任何网络环境
- 相对安全，不需要开放端口

#### 缺点
- 需要自己的公网服务器运行frp
- 有额外延迟（经过中转）
- 需要在路由器安装frp客户端
- 服务器有带宽成本

#### 适用场景
- 没有公网IP的用户
- 有自己VPS的用户
- 对延迟不敏感的用户

#### 实现难度：⭐⭐⭐（中等）

---

### 方案3：云端中转（推荐长期方案）

#### 原理
```
[手机APP] --WebSocket--> [云端服务器] --WebSocket--> [路由器客户端]
```

#### 优点
- 不需要公网IP
- 路由器在内网也能管理
- 安全性高，不需要开放端口
- 可以统一管理多台路由器
- 支持消息推送

#### 缺点
- 需要开发和维护云端服务
- 有服务器成本
- 延迟比直连高
- 实现复杂度高

#### 适用场景
- 没有公网IP的用户
- 需要管理多台路由器
- 追求最佳用户体验
- 商业产品场景

#### 实现难度：⭐⭐⭐⭐⭐（复杂）

---

### 方案4：VPN接入

#### 原理
```
[手机APP] --VPN隧道--> [路由器VPN服务端] ---> [LuCI]
```

#### 优点
- 安全性最高，加密传输
- 不需要开放LuCI端口
- 可以访问整个局域网

#### 缺点
- 配置复杂，需要VPN知识
- 使用不方便，每次需要先连VPN
- 手机端VPN配置繁琐
- 部分网络环境会屏蔽VPN

#### 适用场景
- 对安全要求极高的用户
- 企业级场景
- 技术能力强的用户

#### 实现难度：⭐⭐⭐⭐（较复杂）

---

## 三、方案对比表

| 维度 | DDNS+端口映射 | 内网穿透 | 云端中转 | VPN |
|------|-------------|---------|---------|-----|
| 需要公网IP | ✅ 是 | ❌ 否 | ❌ 否 | ❌ 否 |
| 实现难度 | ⭐⭐ | ⭐⭐⭐ | ⭐⭐⭐⭐⭐ | ⭐⭐⭐⭐ |
| 延迟 | 低（直连） | 中（中转） | 中（中转） | 中（加密） |
| 安全性 | 中（开放端口） | 较高 | 高 | 最高 |
| 成本 | 低（免费DDNS） | 中（VPS） | 高（云服务） | 低 |
| 使用便捷性 | 高 | 高 | 最高 | 低 |
| 多设备管理 | 一般 | 一般 | 优秀 | 一般 |
| 推荐指数 | ⭐⭐⭐⭐ | ⭐⭐⭐ | ⭐⭐⭐⭐⭐ | ⭐⭐ |

---

## 四、推荐方案

### 基础版本：DDNS + 端口映射（已实现）

**理由**：
1. 实现最简单，快速上线
2. 大部分宽带用户有公网IP（或可申请）
3. 延迟最低，体验最好
4. 不需要额外的服务器成本
5. 用户已有DDNS配置经验

### 长期演进：云端中转（后续版本）

**理由**：
1. 不需要公网IP，覆盖更多用户
2. 安全性更高，不需要开放端口
3. 可以统一管理多台路由器
4. 支持消息推送等高级功能
5. 商业产品的标准方案

---

## 五、APP端功能实现

### 已实现功能

#### 1. 远程管理配置页面（RemoteManageScreen）
- 远程管理开关
- 远程地址配置（域名/IP）
- 远程端口配置
- 协议选择（HTTPS/HTTP）
- 自动切换远程模式开关
- 强制远程模式开关
- 测试远程连接按钮
- 技术方案说明
- 路由器配置指引

#### 2. 路由器数据模型扩展（Router）
新增字段：
- `remoteEnabled: Boolean` - 是否启用远程管理
- `remoteAddress: String` - 远程地址（域名/IP）
- `remotePort: Int` - 远程端口（默认443）
- `remoteProtocol: String` - 协议（https/http）
- `autoSwitchRemote: Boolean` - 自动切换远程模式

新增方法：
- `getEffectiveAddress(isRemoteMode: Boolean)` - 获取当前应使用的地址

#### 3. 远程管理ViewModel（RemoteManageViewModel）
- 加载/保存远程配置
- 网络环境检测（WiFi/移动数据）
- 自动切换本地/远程模式
- 测试远程连接
- 继承BaseViewModel，支持网络状态监听

#### 4. 导航集成
- ProfileScreen添加"远程管理"入口
- MainActivity添加RemoteManage路由

### 待实现功能（后续版本）

#### 1. 全局远程模式集成
- 所有ViewModel连接时自动使用远程地址
- 网络状态变化时自动重连
- 连接失败时自动切换本地/远程重试

#### 2. 云端中转支持
- WebSocket长连接
- 云端API对接
- 消息推送

#### 3. 内网穿透支持
- frp配置生成
- 一键部署frp客户端

---

## 六、路由器固件端配置

### 方案1：DDNS + 端口映射配置

#### 1. DDNS配置
支持的服务商：
- 花生壳（oray.com）
- 阿里云（aliyun.com）
- Cloudflare
- No-IP（noip.com）
- 其他标准DDNS服务商

配置脚本：`remote_manage_setup.sh`

#### 2. HTTPS配置
```bash
# 安装HTTPS支持
opkg update
opkg install uhttpd-mod-tls px5g-wolfssl

# 启用HTTPS重定向
uci set uhttpd.main.redirect_https="1"
uci commit uhttpd
/etc/init.d/uhttpd restart
```

#### 3. 防火墙端口开放
```bash
# 开放443端口
uci set firewall.remote_mgmt=redirect
uci set firewall.remote_mgmt.name="Remote Management"
uci set firewall.remote_mgmt.src="wan"
uci set firewall.remote_mgmt.src_dport="443"
uci set firewall.remote_mgmt.dest="lan"
uci set firewall.remote_mgmt.dest_ip="192.168.1.1"
uci set firewall.remote_mgmt.dest_port="443"
uci set firewall.remote_mgmt.proto="tcp"
uci set firewall.remote_mgmt.enabled="1"
uci commit firewall
/etc/init.d/firewall restart
```

#### 4. 安全加固
```bash
# 修改强密码
passwd root

# 禁用SSH密码登录
uci set dropbear.@dropbear[0].PasswordAuth='0'
uci commit dropbear
/etc/init.d/dropbear restart

# 使用非标准端口（如8443）
# 修改防火墙和uhttpd配置
```

### 一键配置脚本
使用 `remote_manage_setup.sh` 可以一键完成上述配置。

---

## 七、安全考虑

### 1. 传输安全
- ✅ 推荐使用HTTPS加密传输
- ✅ APP支持自签名证书
- ⚠️ HTTP模式下数据明文传输，不推荐

### 2. 认证安全
- ✅ 密码加密存储（AES加密）
- ✅ 建议使用强密码
- ⚠️ 开放端口后会受到暴力破解攻击

### 3. 网络安全
- ✅ 可以限制访问IP
- ✅ 可以使用非标准端口
- ⚠️ 公网暴露有被扫描的风险

### 4. 最佳实践
1. 始终使用HTTPS
2. 使用强密码并定期更换
3. 使用非标准端口（如8443）
4. 限制可访问的IP范围
5. 定期检查登录日志
6. 及时更新固件和软件包
7. 禁用不必要的服务

---

## 八、架构图

### 当前架构（DDNS + 端口映射）

```
┌─────────────┐         互联网          ┌─────────────┐
│             │ ──────────────────────> │             │
│  手机APP    │    HTTPS://域名:端口     │  路由器     │
│             │ <────────────────────── │  LuCI       │
└─────────────┘    加密JSON数据         └─────────────┘
       ↑                                      ↑
       │                                      │
       │ 本地模式                              │ DDNS更新
       │                                      │
┌─────────────┐                        ┌─────────────┐
│  局域网WiFi  │ <───────────────────── │  DDNS服务商  │
└─────────────┘                        └─────────────┘
```

### 未来架构（云端中转）

```
┌─────────────┐      WebSocket       ┌─────────────┐      WebSocket       ┌─────────────┐
│             │ ───────────────────> │             │ <─────────────────── │             │
│  手机APP    │                      │  云端服务器  │                      │  路由器     │
│             │ <─────────────────── │             │ ───────────────────> │  客户端     │
└─────────────┘      加密消息         └─────────────┘      加密消息         └─────────────┘
                                                      ↑
                                                      │
                                               ┌─────────────┐
                                               │  消息推送    │
                                               │  设备管理    │
                                               └─────────────┘
```

---

## 九、实现步骤总结

### 已完成
1. ✅ 技术方案分析和对比
2. ✅ Router模型添加远程管理字段
3. ✅ RemoteManageScreen配置页面
4. ✅ RemoteManageViewModel
5. ✅ 导航集成（Profile入口 + MainActivity路由）
6. ✅ 路由器端配置脚本
7. ✅ 技术方案文档

### 待完成
8. ⏳ 编译验证
9. ⏳ 全局远程模式集成（后续版本）
10. ⏳ 云端中转方案（后续版本）

---

## 十、使用说明

### 用户配置流程
1. 路由器端运行 `remote_manage_setup.sh` 配置DDNS和端口
2. 打开APP → 我的 → 远程管理
3. 开启"启用远程管理"
4. 填写DDNS域名、端口、协议
5. 点击"测试远程连接"验证
6. 开启"自动切换远程模式"
7. 完成，APP会自动根据网络环境切换

### 验证方法
- 关闭WiFi，使用移动数据
- 打开APP，应自动切换到远程模式
- 各项功能应正常工作

---

**文档版本**：v1.0
**最后更新**：2026-08-12
